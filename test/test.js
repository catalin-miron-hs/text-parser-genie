// Still have to write tests

/*var TextParserGenie = require('./index');
const fs = require('fs');

var query = [
			{
				'lineNumber': 2,
				'subString': {
								'startIndex': 0,
								'endIndex': {
												'indexOf': 'PROVIDER'
											}
							}
			},
			{
				'field': 'CHECK/EFT #'
			},
			{
				'section': 'TOTALS:',
				'header': 'CHECK',
				'offset': '2'
			}
			];

TextParserGenie({'file': './test/41969.txt', 'wordDelimitter': ' '})
	.parse( query, function(err, results, exceptions){
		console.log(results);
	}
);


TextParserGenie({'file': './test/36847.txt', 'wordDelimitter': ' '})
	.parse( query, function(err, results, exceptions){
		console.log(results);
	}
);*/
